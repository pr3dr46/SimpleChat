var path = require('path');
var fs = require('fs');
var lib = path.join(path.dirname(fs.realpathSync(__filename)), '../simplechat/lib');

var users = require(lib + '/users.js');

module.exports = {
	addUser: users.addUser,
	deleteUser: users.deleteUser,
	getUsers: users.getUsers
}